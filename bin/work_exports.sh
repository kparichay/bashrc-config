# Working directory
export WORKING=/home/kparichay/Working

# exports for nnstreamer build
export NNSUITE_ROOT=$WORKING/nnsuite
export NNST_ROOT=$NNSUITE_ROOT/nnstreamer
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$NNST_ROOT/lib
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$NNSUITE_ROOT/nntrainer_pk/build/nntrainer
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:$NNSUITE_ROOT/nntrainer_pk/build/nnstreamer/tensor_filter
export GST_PLUGIN_PATH=$GST_PLUGIN_PATH:$NNST_ROOT/lib/gstreamer-1.0
export GST_PLUGIN_PATH=$GST_PLUGIN_PATH:$NNST_ROOT/lib/nnstreamer
export GST_PLUGIN_PATH=$GST_PLUGIN_PATH:$NNST_ROOT/lib
export GST_PLUGIN_PATH=$GST_PLUGIN_PATH:$NNSUITE_ROOT/nntrainer_pk/build/nnstreamer/tensor_filter
export GST_PLUGIN_PATH=$GST_PLUGIN_PATH:$NNSUITE_ROOT/nntrainer_pk/build/nntrainer
export NNSTREAMER_FILTERS=$NNST_ROOT/lib/nnstreamer/filters:$NNSUITE_ROOT/nntrainer_pk/build/nnstreamer/tensor_filter:$NNSUITE_ROOT/nntrainer_pk/build/nntrainer
export NNSTREAMER_DECODERS=$NNST_ROOT/lib/nnstreamer/decoders

# export for nnstreamer android build
export JAVA_HOME=$WORKING/Tools/android-studio/jre            # JRE path in Android Studio
export ANDROID_DEV_ROOT=$NNSUITE_ROOT/android
export ANDROID_SDK=$ANDROID_DEV_ROOT/tools/sdk
export ANDROID_HOME=$ANDROID_SDK
export GSTREAMER_ROOT_ANDROID=$ANDROID_DEV_ROOT/gstreamer-1.0
export NNSTREAMER_ROOT= $NNSUITE_ROOT/nnstreamer_pk
export ANDROID_NDK=$ANDROID_DEV_ROOT/android-ndk-r21d
export PATH=$ANDROID_NDK:$PATH
export PATH=$ANDROID_ROOT/bin:$PATH

# build/indent nnstreamer
alias bnnst='/home/kparichay/Working/nnsuite/build_script.sh'
alias innst='indent \
  --braces-on-if-line \
  --case-brace-indentation0 \
  --case-indentation2 \
  --braces-after-struct-decl-line \
  --line-length80 \
  --no-tabs \
  --cuddle-else \
  --dont-line-up-parentheses \
  --continuation-indentation4 \
  --honour-newlines \
  --tab-size8 \
  --indent-level2 \
  --leave-preprocessor-space'
#indent nntrainer
alias cnnt='find . \( -name '*.c' -o -name '*.cpp' -o -name '*.h' \) -exec clang-format-6.0 -i {} +'

## Tizen SDK configuration
# This is generated by Tizen SDK. Please do not modify by yourself.
export PATH=$PATH:/home/kparichay/Working/tizen-studio/platforms/tizen-5.0/common/tidl
alias sdb='/home/kparichay/Working/tizen-studio/tools/sdb'

# cerbero
alias cerbero='/home/kparichay/Working/Tools/cerbero/cerbero-uninstalled'

# for NPU
export PATH=/opt/trinity/npu-scc/bin/:$PATH

# Add the directory of Tizen .NET Command Line Tools to user path.
export PATH=/home/kparichay/Working/tizen-studio/tools/ide/bin:$PATH
